
import { useToast as useToastComponent } from "@/components/ui/use-toast";
import { toast as toastComponent } from "@/components/ui/toast";

export const useToast = useToastComponent;
export const toast = toastComponent;
